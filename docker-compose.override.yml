version: "3"

services:
  web:
    volumes:
      - ./src:/app
    command: [ "flask", "run", "--host=0.0.0.0", "--port=80" ]
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app

  worker:
    volumes:
      - ./src:/app
      - ./storage:/storage
