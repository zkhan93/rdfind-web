version: "3"

services:
  server:
    volumes:
      - ./server:/app
    command: [ "flask", "run", "--host=0.0.0.0", "--port=80" ]
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app

  worker:
    volumes:
      - ./server:/app
      - ./storage:/storage
